"""QA Agent — autonomous QA orchestration engine.

The Python package that the qa-master agent and the qa-agent CLI delegate to.
Everything the LLM does NOT do (scanning, parsing, execution, state, reports)
lives here.
"""

__version__ = "2.0.0"
