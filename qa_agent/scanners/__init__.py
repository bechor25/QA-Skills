"""Scanners: read the target project and emit state files.

Scanners are pure-Python and deterministic — no LLM calls. They produce
the inputs that the intelligence layer (risk, strategy) reasons over.
"""
