"""CLI command implementations.

Each module exposes a top-level `run(args)` that returns an int exit code.
The shells here are intentionally thin — the real work lives one layer
down (scanners, engines, executors, report).
"""
